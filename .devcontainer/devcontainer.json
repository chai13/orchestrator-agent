{
    "name": "Orchestrator Agent Dev Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "mounts": [
        "source=${env:HOME}/.ssh,target=/root/.ssh,type=bind",
        "source=/root/.mtls,target=/root/.mtls,type=bind",
        "source=orchestrator-shared,target=/var/orchestrator,type=volume"
    ],
    "features": {
        "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
    },
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            },
            "extensions": [
                "ms-vscode.cpptools",
                "ms-python.python",
                "esbenp.prettier-vscode",
                "mahatolalit.readme-previewer"
            ]
        }
    },
    "containerEnv": {
        "HOST_NAME": "orchestrator-agent-devcontainer"
    },
    "forwardPorts": [22, 7676],
    "postCreateCommand": "",
    "remoteUser": "root",
    "runArgs": ["--name", "orchestrator-agent-devcontainer"]
}